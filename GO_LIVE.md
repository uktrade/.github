## Setup tagging on repo
TBC

## Setup pre-commit hooks

### Repository already has pre-commit library installed?
Append the dbt security hook repo to the `.pre-commit-config.yaml` file

### Repository library needs to install pre-commit library?
- Add pre-commit to repo using pip / package manager used in repo
- Create a `.pre-commit-config.yaml` file, add the dbt security hook to it

Install trufflehog cli????